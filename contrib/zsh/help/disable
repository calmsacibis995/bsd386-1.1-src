     disable arg ...
          Disable the builtin arg temporarily.  This  allows  you
          to  use  an  external  command  with the same name as a
          shell builtin.  Actually the same as unhash.   Builtins
          can be enabled with the enable command.
