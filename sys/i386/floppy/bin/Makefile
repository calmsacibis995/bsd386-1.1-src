all obj depend install tags:

BINS= cat compress cp dd diskdefect disklabel disksetup fsck ftp \
	ifconfig init installsw ls mkdir mknod mount mount_iso9660 \
	mt mv newfs ping pwd restore rm route rsh scsicmd sh \
	slattach stty sync tar telnet umount vi gzip ln pax tail test chmod

clean cleandir:
	-rm -f ${BINS}
